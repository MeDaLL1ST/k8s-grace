apiVersion: v1
kind: Service
metadata:
  name: graceful-app-server-shutdown
spec:
  selector:
    app: graceful-app-server-shutdown
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
