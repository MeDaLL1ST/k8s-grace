apiVersion: v1
kind: Service
metadata:
  name: graceful-app-baseline
spec:
  selector:
    app: graceful-app-baseline
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
